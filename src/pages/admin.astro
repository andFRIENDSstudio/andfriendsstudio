---
export const prerender = false;

// In development, redirect to Tina's dev server
if (import.meta.env.DEV) {
  return Astro.redirect('http://localhost:4001/admin/index.html');
}
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>&FRIENDS Admin</title>
    <script type="module">
      // In production, redirect to built admin interface
      window.location.href = '/admin/index.html';
    </script>
  </head>
  <body>
    <p>Redirecting to admin...</p>
  </body>
</html>
```

---

## **Step 3: Move Tina credentials to environment variables**

**Create `.env` file in your project root:**
```
